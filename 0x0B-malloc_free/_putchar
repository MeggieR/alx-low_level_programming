#include "main.h"
#include <unistd.h>
/**
*_putchar -prints character c to stdout
*@c:The character to br printed
*Return: On success 1, 0 on error
**/
int _putchar(char c)
{
return (write(1, &c, 1));
}
